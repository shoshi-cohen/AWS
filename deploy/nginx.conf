server {
    listen 80 default_server;
    server_name _;

    root /var/www/html;
    index index.html;

    location /api {
        proxy_pass http://localhost:4000;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
